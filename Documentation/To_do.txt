Login System:
*****FIX ENTRIES*****

HEADER:
- Live time, Add timetable, remove timetable, drop down menu change timetable
FOOTER:
- UN: Username, UID (UID), Session (session timing)
LHS:
- Menu bar: Logout, Stopwatch window
RHS:
- Interactive Calendar
Centre:
- Timetable, fetch from Excel file, interactive tiles: new window log homework (with colours?)



Database:

ADD NEW TABLE "NoteTbl" , FIELDS:
(note_id NULL) ,
(user_id FOREIGN REFERENCES UsersTbl(users)) ,
(date TEXT),
(notes TEXT)


New Window "Tasks":

Opens List of tasks to do by date, Grabs date from calendar on main program window


Reset Password screen, pass by security code
